# 数学建模助手 - 规格文档

## 1. 项目概述

- **项目名称**: 数学建模助手 (Math Modeling Assistant)
- **项目类型**: Web 单页应用
- **核心功能**: 对数学建模题目进行智能分析，提供模型建议、可行性分析、推荐建模软件及数据筛选建议
- **目标用户**: 数学建模参赛者、学习者

## 2. UI/UX 规格

### 布局结构

```
┌─────────────────────────────────────────────────────────────┐
│  Header: 数学建模助手                              [设置]   │
├────────────────────────────┬────────────────────────────────┤
│                            │                                │
│   左侧面板 (用户输入)       │    右侧面板 (AI 响应)           │
│                            │                                │
│   - 题目输入区             │    - 分析结果展示               │
│   - 图片上传区             │    - 模型建议                   │
│   - 发送按钮               │    - 可行性分析                 │
│                            │    - 软件推荐                   │
│                            │    - 数据筛选建议               │
│                            │                                │
└────────────────────────────┴────────────────────────────────┘
```

- 响应式设计：移动端垂直堆叠，桌面端左右分栏
- 固定 Header，分栏布局

### 视觉规格

**颜色方案**
- 主色: `#1a1a2e` (深蓝黑)
- 次要色: `#16213e` (深蓝)
- 强调色: `#e94560` (玫红)
- 背景色: `#0f0f23` (极深蓝黑)
- 文字色: `#eaeaea` (浅灰白)
- 次要文字: `#8892b0` (灰蓝)
- 边框色: `#2d2d44` (暗紫灰)

**字体**
- 标题: "Noto Sans SC", system-ui, sans-serif
- 正文: "Noto Sans SC", system-ui, sans-serif
- 代码/等宽: "JetBrains Mono", "Fira Code", monospace

**间距系统**
- 基础单位: 8px
- 内边距: 16px / 24px / 32px
- 分栏间隙: 1px (分隔线)

**视觉效果**
- 玻璃拟态卡片效果
- 柔和阴影: `0 8px 32px rgba(0, 0, 0, 0.3)`
- 圆角: 12px (卡片), 8px (按钮/输入框)
- 分栏分隔线: 1px solid `#2d2d44`

### 组件规格

**Header**
- 高度: 64px
- 左侧: Logo + 标题
- 右侧: API Key 设置按钮

**左侧面板 - 输入区**
- 题目类型切换: 文字输入 / 图片上传 (Tab 切换)
- 文字输入: textarea, placeholder "请输入数学建模题目..."
- 图片上传: 拖拽区域 + 点击上传，支持 JPG/PNG/GIF/WebP
- 发送按钮: 强调色，hover 发光效果

**右侧面板 - 响应区**
- 标题区: 显示分析模块
- 内容区: Markdown 渲染，支持代码高亮
- 加载状态: 脉冲动画 + "正在分析..." 文字

**设置弹窗**
- 模态框，居中显示
- API Key 输入框 (密码类型)
- 保存/取消按钮

## 3. 功能规格

### 核心功能

1. **题目输入**
   - 支持文本直接输入
   - 支持图片上传 (截图、拍照、文件选择)
   - 支持拖拽图片到上传区

2. **AI 分析输出**
   - 题目分析：理解题目背景、目标、约束条件
   - 模型建议：推荐合适的数学模型
   - 可行性分析：评估模型实施难度、数据需求
   - 推荐软件：MATLAB/Python/R/SPSS 等
   - 数据筛选：分析需要哪些数据、如何预处理

3. **API Key 管理**
   - 本地存储 API Key
   - 安全输入，不明文显示

### 交互规格

- 发送按钮: 点击后显示加载状态，禁用重复提交
- 图片上传: 预览缩略图，支持删除重新上传
- Markdown 渲染: 支持代码块、列表、粗体等
- 滚动同步: 响应内容区可独立滚动

### 数据处理

- 图片转 Base64 发送给 API
- API 调用: Minimax API (MiniMax-Text-01)
- 错误处理: 网络错误、API 错误、余额不足等

## 4. 验收标准

- [ ] 页面正常加载，无 JS 错误
- [ ] 左右分栏布局正常显示
- [ ] 文字输入功能正常
- [ ] 图片上传和预览正常
- [ ] API 调用成功返回分析结果
- [ ] Markdown 内容正确渲染
- [ ] 加载状态正常显示
- [ ] API Key 设置弹窗正常
- [ ] 响应式布局在移动端正常
- [ ] 视觉风格符合深色主题规格
